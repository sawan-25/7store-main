import React from 'react'
import { developerName } from '../Utils/constants';

const DeveloperInfo = () => {
    
  return (
    <div>
      {developerName}
    </div>
  )
}

export default DeveloperInfo
